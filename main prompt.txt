Act as an expert deep learning instructor teaching a student who already knows Python, basic neural networks (perceptrons, backpropagation, activation functions), and has used PyTorch/TensorFlow before. Teach me Convolutional Neural Networks (CNNs) completely, from first principles to advanced/interview-level depth. Structure the lesson as follows:

1. INTUITION FIRST
   - Explain, with a real-world analogy, why CNNs exist and what problem they solve that plain fully-connected networks can't (spatial structure, parameter explosion, translation invariance).

2. CORE BUILDING BLOCKS (one section each, with math + intuition + diagram description + code)
   - Convolution operation (explain kernels/filters, stride, padding — valid vs same)
   - Output size formula: derive and show  O = floor((W - K + 2P)/S) + 1
   - Feature maps and channels (how depth changes across layers)
   - Activation functions in CNN context (ReLU and why it's preferred here)
   - Pooling (max pooling, average pooling) — why we downsample, math of output size
   - Flattening and transition to fully connected layers
   - Softmax output layer for classification

3. THE MATH
   - Full forward pass equations for a convolution layer (with bias term)
   - Backpropagation through a conv layer — how gradients flow back through kernels (derive at least the key gradient equations, don't just state them)
   - Backpropagation through a max pooling layer (routing gradients only through the max index)
   - Parameter counting formula for a conv layer vs an equivalent dense layer, with a worked numeric example showing the parameter savings

4. CODE — BUILD A CNN FROM SCRATCH
   - A minimal CNN in PyTorch (or TensorFlow/Keras, your choice) for MNIST or CIFAR-10 classification, fully annotated line-by-line
   - Show the model.summary() / parameter count output and explain each layer's shape transformation
   - Include a short training loop with loss/optimizer setup

5. KEY ARCHITECTURAL CONCEPTS
   - Receptive field (what it is, how it grows with depth, why it matters)
   - 1x1 convolutions and what they're actually used for
   - Batch normalization in CNNs (why, where it's placed, brief math)
   - Dropout in CNN context
   - Data augmentation techniques for images (flip, rotation, crop, color jitter) and why they act as regularization
   - Transfer learning with pretrained CNNs (feature extraction vs fine-tuning)

6. FAMOUS ARCHITECTURES — brief comparative overview
   - LeNet-5, AlexNet, VGG, ResNet (explain skip connections and the vanishing gradient problem they solve), Inception (why multiple kernel sizes), EfficientNet (compound scaling)
   - Present this as a comparison table: name, year, key innovation, depth, parameter count order of magnitude

7. COMMON PITFALLS & DEBUGGING
   - Overfitting signs specific to CNNs and how to address them
   - Vanishing/exploding gradients in deep CNNs
   - Common shape-mismatch errors and how to debug them

8. 20 MOST-ASKED CNN INTERVIEW QUESTIONS
   - List 20 real interview questions ranging from conceptual ("why do CNNs use shared weights?") to mathematical ("derive the gradient of a conv layer") to practical ("how would you reduce overfitting in a CNN with limited data?") to architecture-comparison questions
   - For each question, also give a concise 2-4 sentence model answer

9. INDEX / SUMMARY
   - End with a clean bullet-point index of every concept covered in this lesson, organized under the same section headers above, so I can use it as a revision sheet

Use clear headers, code blocks with syntax highlighting, and don't skip derivations — show your work on any formula rather than just stating the result. Where useful, describe what a diagram would look like even though you can't draw one directly.